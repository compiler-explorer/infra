name: Some kind of one-off build

# really on demand
on: [push]

jobs:
  buildcross:
    name: OHAI
    runs-on: ['ce','ubuntu']

    steps:
    # todo get revision
    - name: Build compiler
      run: |
        mkdir /tmp/compiler-out
        docker run --rm -v/tmp/compiler-out:/tmp/compiler-out compilerexplorer/gcc-builder bash build.sh 11.2.0 /tmp/compiler-out/ ""
    - name: ls the output
      run: ls /tmp/compiler-out
    # todo upload s3 thing
