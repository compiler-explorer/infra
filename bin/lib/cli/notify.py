# Legacy notify CLI commands removed
# Notifications are now handled automatically by the blue-green deployment system
# Use 'ce blue-green deploy --check-notifications' to preview notifications
# Use 'ce blue-green deploy --dry-run-notify' to test notification system
