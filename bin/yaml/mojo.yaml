compilers:
  mojo:
    type: pip
    dir: mojo-{name}
    python: /usr/bin/python3
    package: modular
    check_exe: mojo --version
    after_stage_script:
      - set -e
      - python3 -m pip install modular

