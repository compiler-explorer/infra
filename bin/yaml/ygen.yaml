compilers:
  ygen:
    ylc:
      type: ziparchive
      fetch: https://github.com/Cr0a3/ygen/tree/build
      script: |
        cp ygen-build/ygen-build/* ygen/*
      check_exe: ylc --version --in="abc"
      dir: ygen/bin
      targets:
        - newest
